[project]
name = "lora-model"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mlx-lm>=0.24",
    "pyyaml>=6.0.3",
    "transformers<5.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
